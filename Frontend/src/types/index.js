/**
 * Shared domain type shapes (JSDoc). Migrate to TypeScript incrementally.
 * @typedef {Object} ForecastResult
 * @property {number} predicted_customers
 * @property {number} [confidence]
 * @property {string} [model_version]
 */

export {}
